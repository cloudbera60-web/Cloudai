FROM node:20-alpine

WORKDIR /app

RUN apk add --no-cache python3 make g++ git


COPY package.json ./


RUN npm install


COPY . 

RUN mkdir -p temp

ENV NODE_ENV=production
ENV PORT=8000

EXPOSE 8000


CMD ["node", "index.js"]
